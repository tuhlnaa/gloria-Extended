data:
  dataset: chexpert
  # fraction: 1.0  # Optional fraction of data to use (for training)
  image:
    imsize: 256

# Uses transforms built from config
transforms:  
  random_crop:
    crop_size: 224
  # random_horizontal_flip: 0.5
  # random_affine: null
  # color_jitter: null
  # norm: norm

train:
  batch_size: 16  # Batch size
  num_workers: 0  # Number of workers

path:
  data_dir: "D:\\Kai\\DATA_Set_2\\X-ray\\CheXpert-v1.0-small"
  train_csv: "train.csv"  # Path to the specific split CSV file
  valid_csv: "valid.csv"
  test_csv: "valid.csv"   # Using validation set as test set (test set labels hidden)